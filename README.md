# TFVC-Workspace-Manager
TFVC Workspace Manager es una herramienta interna para administrar y operar sobre workspaces de TFVC en Azure DevOps. Permite consultar Team Projects, workspaces, cambios pendientes y locks, además de ejecutar acciones como undo de pending changes y unlock de elementos bloqueados, con soporte de autenticación, logging y auditoría.
